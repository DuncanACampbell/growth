{
  "name": "growth-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && npx firebase-tools emulators:start --only functions --project growth-b8456"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0",
    "openai": "^7.5.0"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
